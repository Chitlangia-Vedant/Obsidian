- Trace precedents And Trace dependents :- Shows how the cells are connected to each other through formulas
- =Large() OR =Small() :- Find nth largest or smallest value in given range
- =Averageifs() :- Average with criteria
- =Lookup() OR =Vlookup() :- Import specific data from different worksheet or workbook
- =Choose() :- Output a certain value based on index(input) no.
- =Indirect() :- Use the Character in a cell as cell reference in a formula
- =Index() :- Return a value of a cell at an intersection of a row and column in a riven range
- =Match() :- Return a relative position of an item in an array
- For Date Formate
    
    - =Year()
    - =Day()
    - =Eomonth() :- End of month
    - =Text(A1,"ddd") OR =Text(A1,"dddd") :- Week day for particular date
    - =Text(A1,"mmm") OR =Text(A1,"mmmm") :- Month name for particular date
- =Concatenate() :- Add text of diff. cell