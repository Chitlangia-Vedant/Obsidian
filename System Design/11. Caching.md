Caching: Storing/Precomputing data at some place so as to not fetch it again and again from original source & thus to overall improve the performance of my application.
# Types of Caching
## In Browser Caching
### DNS values
Store the DNS values in local storage.
So we don't have to frequently access DNS server
### Images (static files/images)
Store images/files in local storage

## CDNs (Content Delivery Network)

## Local Caching 
Caching data in Application server

Pros
- Reduce network overhead (No DB-Server call)
- Reduce disk fetch time

## Global Caching
Redis

# Problems regarding caching

Cache is never the original source of data, it is copy of the original data.

- **Cache Invalidation**
	Original Source (DB) and the copy (cache) may get out of sync.
- **Cache Eviction**
	- Original Source: DB: HDD
	- Copy: Cache: RAM
	- Space/Cost (HDD) >>> Space/Cost (RAM)
	- Cache may get full, therefore to store a new entry we need to remove existing entry.
	- What to remove?

# Cache Invalidation: Keeping original & copy of data in SYNC.

## Time To Live (TTL)

Whenever data is stored in cache, set TTL along with it.
After TTL is expired data will get auto deleted.

TTL approach can only be used if you're okay with a bit stale data.
### Read
1. Is data in cache?
	1. Yes -> Fetch from the cache & return
	2. No
		1. Fetch the data from DB
		2. Store the data in cache with TT
		3. Return the data

### Write
UpdateDB(...)

## Writing Strategies

Cache become stale whenever DB is updated.
Different ways of writing will impact how DB and cache are in sync.
### Write Through Cache
1. Writes the data to cache
	1. Fails -> Return FAILURE
	2. Succeeds -> Write the data to DB
		1. Fails -> Remove the data from cache & return FAILURE
		2. Succeeds -> Return SUCCESS

Properties
- DB & Cache will never be out of sync.
- Slow Write Queries
### Write Back Cache
1. Write data to cache
2. Return SUCCESS
3. Async. try to write to DB

Properties
- Writes may get lost
- This works in scenarios where data loss is acceptable.
### Write Around Cache (TTL)

Just write in DB

|                     | Consistency            | Write      | Cons                     |
| ------------------- | ---------------------- | ---------- | ------------------------ |
| Write Through Cache | Always Consistence     | Cache + DB | Writes are slower        |
| Write Around Cache  | Eventually Consistence | DB         | Stale data for some time |
| Write Back Cache    | May not be Consistence | Cache      | Data may get lost        |

-> Consistency -> 
Write Through, Write Around, Write Back
<- Availability <- 

# Cache Eviction: Handle cache getting full

**Cache Eviction Strategies:** Algorithm to decide what data to remove from cache when its full.

1. Least Recently Used (LRU)
2. Least Frequently Used (LFU)
3. First In First Out (FIFO)
4. Most Recently Used (MRU)
5. last In First Out (LIFO)

Decide best algorithm by what leads to least **Cache Miss**