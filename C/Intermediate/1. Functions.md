→ A function is a self contained block of statements that perform a task whenever called.

#Syntax 

```c
#include<stdio.h>

void function1();//void signify no output(return) will be given
int function2(int ,int *);//(int,int *) signify the tye of input

int main()
{
.
.
function1();//calling the funtion1
.
.
a=function2(b,c);//b is int & c is pointer of int
.
.
return 0;
}
void function1()
{
statments;
return;
}
int function(int input1,int *input2)
{
statements;
return output;
}
```

#IMP 
	- Any function can be called from any other function. Even main() can be called from other functions.
	- In C, during the function call, the argument are passed from right to left.

# Recursion

→A function is called 'recursive' if a statement within the body of a function call the same function.