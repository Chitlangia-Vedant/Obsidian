- `sizeof(x)`
	It gives no. of byte occupied by an entity
	
- increment
	- i++<10
		First comparison & then increment
		
	- ++i<10
		First increment & then comparison
		
- break Statement
	-When break is encountered inside any loop, control automatically passes to the first statement after loop.
	
- continue Statement
	-When continue is encountered inside any loop, control automatically passes to the  beginning of the loop
	
- exit()
	exit() is a standard library fun that terminates the execution of the program.
	
- fflush(stdin)
	-flush the 'enter' in buffer when `scanf()` is used

-  goto Keyword
	→*Do not use **goto** as much as possible*
	
	#Syntax 
	
	```c
	goto sos;
	.
	.
	.
	sos : 
		printf("....");
	return 0;
	```

